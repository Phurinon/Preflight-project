name: preflight-g7-db
services:
  postgres:
    image: postgres:16-alpine
    container_name: pf-db-g7
    ports:
      - ${POSTGRES_PORT}:5432
    volumes:
      - pf-data:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_DB=${POSTGRES_DB}
    networks:
      - pf-net
  backend:
    depends_on:
      - postgres
    image: werasak/preflight-backend-g7:latest
    container_name: pf-backend-g7
    ports:
      - ${PORT}:3000
    networks:
      - pf-net
    environment:
      - POSTGRES_DB=${POSTGRES_DB}
      - POSTGRES_PORT=${POSTGRES_PORT}
      - POSTGRES_HOST=${POSTGRES_HOST}
      - POSTGRES_APP_USER=${POSTGRES_APP_USER}
      - POSTGRES_APP_PASSWORD=${POSTGRES_APP_PASSWORD}
  frontend:
    depends_on:
      - backend
    image: werasak/preflight-frontend-g7:latest
    container_name: pf-frontend-g7
    ports:
      - ${NGINX_PORT}:5177
    networks:
      - pf-net
    environment:
      - NGINX_PROXY=${NGINX_PROXY}
      - NGINX_PORT=${NGINX_PORT}
volumes:
  pf-data:
networks:
  pf-net: